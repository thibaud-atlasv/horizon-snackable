<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="Transparent" d:DesignWidth="600" d:DesignHeight="600">

  <Viewbox Width="600" Height="600" Stretch="Uniform">
    <Canvas Width="600" Height="600">

      <!-- Coin sprites -->
      <ItemsControl ItemsSource="{Binding coins}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Image Source="Textures/gold_icon.png" Width="50" Height="50"
                   Opacity="{Binding opacity}"
                   RenderTransformOrigin="0.5,0.5">
              <Image.RenderTransform>
                <TransformGroup>
                  <ScaleTransform ScaleX="{Binding scaleX}" ScaleY="{Binding scaleY}"/>
                  <RotateTransform Angle="{Binding rotation}"/>
                  <TranslateTransform X="{Binding x}" Y="{Binding y}"/>
                </TransformGroup>
              </Image.RenderTransform>
            </Image>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <!-- Text items -->
      <ItemsControl ItemsSource="{Binding texts}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding text}"
                       Foreground="{Binding color}"
                       Opacity="{Binding opacity}"
                       FontFamily="Roboto" FontSize="30" FontWeight="Bold"
                       Stroke="#FF000000" StrokeThickness="3"
                       RenderTransformOrigin="0.5,0.5">
              <TextBlock.RenderTransform>
                <TransformGroup>
                  <ScaleTransform ScaleX="{Binding scaleX}" ScaleY="{Binding scaleY}"/>
                  <TranslateTransform X="{Binding x}" Y="{Binding y}"/>
                </TransformGroup>
              </TextBlock.RenderTransform>
            </TextBlock>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

    </Canvas>
  </Viewbox>
</Page>
